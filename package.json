{
  "name": "glacier-tokens-subgraphs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ glacier-fi/glacier-tokens-subgraphs",
    "create-local": "graph create --node http://localhost:8020/ glacier-fi/glacier-tokens-subgraphs",
    "remove-local": "graph remove --node http://localhost:8020/ glacier-fi/glacier-tokens-subgraphs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 glacier-fi/glacier-tokens-subgraphs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
